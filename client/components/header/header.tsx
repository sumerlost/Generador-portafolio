export default function Header({ children }: { children: React.ReactNode }) {
    const componente1: string = ""
    const componente2: string = ""

    return (
        <div>
            {children}
        </div>
    )
}